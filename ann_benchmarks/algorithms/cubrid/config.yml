float:
  any:
  - base_args: ['@metric']
    constructor: CUBVEC 
    disabled: false
    docker_tag: ann-benchmarks-cubrid
    module: ann_benchmarks.algorithms.cubrid
    name: cubrid
    run_groups:
      M-16:
        arg_groups: [{M: 16, efConstruction: 200}]
        args: {}
        query_args: [[20, 40, 80, 100, 200, 400]]
      M-24:
        arg_groups: [{M: 24, efConstruction: 200}]
        args: {}
        query_args: [[20, 40, 80, 100, 200, 400]]
